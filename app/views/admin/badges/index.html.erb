<h1><%= t('.header') %></h1>

<p class="new-test">
  <%= link_to t('.new_badge'), new_admin_badge_url %>
</p>

<%= render 'shared/errors', resource: @badge %>

<table class="table">
  <tr>
    <th>
      <%= t('.title') %>
    </th>
    <th>
      <%= t('.image') %>
    </th>
    <th>
      <%= t('.rule') %>
    </th>
    <th>
      <%= t('.value') %>
    </th>
    <th colspan="3"><%= t('.action') %></th>
  </tr>

  <%= render @badges %>

</table>
