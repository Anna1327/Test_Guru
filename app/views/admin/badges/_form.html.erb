<%= form_with model: [:admin, @badge], local: true do |form| %>
  <%= render 'shared/errors', resource: @badge %>

  <div>Условие выдачи бейджа записывается в поля Rule/Value:
    <p>Примеры:</p>
    <p>rule = "category", value = "Frontend"</p>
    <p>rule = "level", value = "easy"</p>
    <p>rule = "first_try", value = "true"</p>
  </div>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title, id: :badge_title %>
  </p>

  <p>
    <%= form.label :image_url %> <br>
    <%= form.text_field :image_url %>
    <%= @badge.image_url %>
  </p>

  <p>
    <%= form.label :rule %> <br>
    <%= form.text_field :rule, id: :badge_rule_id %>
  </p>

  <p>
    <%= form.label :value %> <br>
    <%= form.text_field :value, id: :badge_value_id %>
  </p>

  <p><%= form.submit %></p>

<% end %>