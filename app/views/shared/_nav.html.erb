<nav class="navbar navbar-light" style="background-color: #e3f2fd;">
  <span>
    <%= link_to t('header-nav.test_guru'), root_path, class: 'navbar-brand' %>
    <% if user_signed_in? %>
      <button type="button" class="btn btn-outline-primary">
      <%= link_to t('header-nav.user_badges'), badges_path, class: 'nav-link' %>
      <% if current_user.admin? %>
        <button type="button" class="btn btn-outline-primary">
          <%= link_to t('header-nav.admin_panel'), admin_tests_path, class: 'nav-link' %>
        </button>
        <button type="button" class="btn btn-outline-primary">
          <%= link_to t('header-nav.admin_badges'), admin_badges_path, class: 'nav-link' %>
        </button>
        <button type="button" class="btn btn-outline-primary">
          <%= link_to t('header-nav.admin_gists'), admin_gists_path, class: 'nav-link' %>
        </button>
      <% end %>
    <% end %>
  </span>
  
  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= t('header-nav.greeting') %>, <b><%= current_user.email %></b> Guru |
      <%= link_to t('header-nav.logout'), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t('.log_in'), new_session_path(resource_name) %> |
      <%= link_to t('.sign_up'), new_registration_path(resource_name) %>
    <% end %>
  </span>
</nav>