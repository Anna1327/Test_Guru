<nav class="navbar navbar-light" style="background-color: #e3f2fd;">
  <span>
    <%= link_to "TestGuru", root_path, class: 'navbar-brand' %>
    <% if user_signed_in? %>
      <button type="button" class="btn btn-outline-primary">
      <%= link_to "User Badges", badges_path, class: 'nav-link' %>
      <% if current_user.admin? %>
        <button type="button" class="btn btn-outline-primary">
          <%= link_to t('header-nav.admin'), admin_tests_path, class: 'nav-link' %>
        </button>
        <button type="button" class="btn btn-outline-primary">
          <%= link_to "Admin Badges", admin_badges_path, class: 'nav-link' %>
        </button>
        <button type="button" class="btn btn-outline-primary">
          <%= link_to "Admin Gists", admin_gists_path, class: 'nav-link' %>
        </button>
      <% end %>
    <% end %>
  </span>
  
  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= t('header-nav.greeting') %>, <b><%= current_user.email %></b> Guru |
      <%= link_to t('header-nav.logout'), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t('.log_in'), new_session_path(resource_name) %> |
      <%= link_to t('.sign_up'), new_registration_path(resource_name) %>
    <% end %>
  </span>
</nav>