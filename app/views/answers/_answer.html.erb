<li>
  <%= link_to answer.body, answer_path(answer)%> |
  <%= link_to t('navigation.edit'), edit_answer_path(answer) %> |
  <%= link_to t('navigation.delete'), answer_path(answer), method: :delete, data: {confirm: 'Are you sure?'} %>
</li>