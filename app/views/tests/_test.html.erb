<% if test.questions.count > 0 %>
  <tr>
    <td><%= test.title %></td>
    <td><%= test_level(test) %></td>
    <td><%= test.questions.count %></td>
    <td><%= button_to t('helpers.submit.start'), start_test_path(test) %></td>
  </tr>
<% end %>